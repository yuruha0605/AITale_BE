apiVersion: networking.k8s.io/v1

kind: Ingress 

metadata:
  name: msa-ingress
  namespace: lgcns

spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: apigateway-service
            port:
              number: 80

      
