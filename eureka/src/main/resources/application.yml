# Config Server version
spring:
  application:
    name: eureka-service
  config:
    import: "optional:configserver:${CONFIG_SERVER_URL:http://localhost:8080}"
  cloud:
    config:
      fail-fast: true
